# ============================================
# FleetCtrl Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values

# --------------------------------------------
# 1. CORE CONFIGURATION
# --------------------------------------------

# [REQUIRED] Convex Deployment
# Retrieve these from your Convex dashboard: https://dashboard.convex.dev
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# [REQUIRED] Convex Site URL
# This is the same as NEXT_PUBLIC_CONVEX_URL but ends in .site instead of .cloud
# Used for BetterAuth integration AND Device Auth
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# [REQUIRED] App Site URL
# Your local development URL or production URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --------------------------------------------
# 2. FEATURE FLAGS
# --------------------------------------------

# [OPTIONAL] Registration Control
# Set to 'false' to hide the "Sign Up" button in the UI.
# Note: You MUST also set ALLOW_REGISTRATION=false in Convex env variables to enforce this on the backend.
NEXT_PUBLIC_ALLOW_REGISTRATION=true

# --------------------------------------------
# 3. PRODUCTION ONLY
# --------------------------------------------

# [OPTIONAL] Convex Deploy Key
# Only needed for CI/CD or production deployments
# CONVEX_DEPLOY_KEY=prod:your-deploy-key

# ============================================
# CONVEX BACKEND VARIABLES
# ============================================
# These variables must be set in the Convex Dashboard or via CLI.
# They are NOT read from .env.local by the backend.

# Run the following commands to set them up:

# 1. Generate and set BetterAuth Secret (Required)
#    npx convex env set BETTER_AUTH_SECRET=$(openssl rand -base64 32)
#    npx convex env set JWT_SECRET=$(openssl rand -base64 32)

# 2. Set Site URL for Backend (Required for Auth & Device Management)
#    npx convex env set SITE_URL http://localhost:3000
#    npx convex env set API_URL https://your-deployment.convex.site

# 3. Enable/Disable Registration (Optional, default: true)
#    npx convex env set ALLOW_REGISTRATION true
